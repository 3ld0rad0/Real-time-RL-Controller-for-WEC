
  # WEC-RL Controller UI Redesign

  This is a code bundle for WEC-RL Controller UI Redesign. The original project is available at https://www.figma.com/design/gL17aCBeoAXeTeLMm7bLvF/WEC-RL-Controller-UI-Redesign.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  